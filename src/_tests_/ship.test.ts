import type * as TestFunction from '../index';

const {  } = jest.requireActual<typeof TestFunction>('../index');

describe('Test ship function', () => {
  it('should sum two numbers', () => {
    
  });
})